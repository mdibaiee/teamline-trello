before_script:
  - mysql -e 'create database teamline_test'
  - npm prune
  - npm install
script:
  - npm run test:single
  - npm run check-coverage
after_success:
  - npm run report-coverage

language: node_js
node_js:
  - "5.6.0"

services: mysql

sudo: false

cache:
  directories:
    - node_modules

compiler: clang-3.6
env:
  - CXX=clang-3.6
addons:
  apt:
    sources:
      - llvm-toolchain-precise-3.6
      - ubuntu-toolchain-r-test
    packages:
      - clang-3.6
      - g++-4.8
